/* Basic styling for a card element */
.card {
    width: 300px;
    padding: 20px;
    background-color: #f5f5f5;
    border-radius: 8px;
    border: 1px solid #ddd;
    font-family: Arial, sans-serif;
}

/* Style the title inside the card */
.card h2 {
    font-size: 20px;
    margin-bottom: 10px;
    color: #333;
}

/* Style a button */
.button {
    padding: 10px 16px;
    background-color: #0078d4;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
}

.button:hover {
    background-color: #005ea6;
}